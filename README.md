AI email responder, built with RAG system and LLM, and with templates for model to follow
npm run dev to start client for localhost
node ./src/index.js to start server for localhost
if want to run on vercel change the api calls on boundaryUI pages only to /api/compose instead of http://localhost:3001/api/compose
